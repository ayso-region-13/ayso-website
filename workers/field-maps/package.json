{
  "name": "ayso13-field-maps",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker serving the AYSO Region 13 field-map editor. Board members (gated by Cloudflare Access) draw field layouts over Mapbox satellite; the Worker commits the rendered PNG + re-editable annotation JSON to the staging branch via the GitHub Git Data API.",
  "scripts": {
    "preflight": "node -e 'if(!process.env.CLOUDFLARE_ACCOUNT_ID){console.error(\"\\n\\u2717 CLOUDFLARE_ACCOUNT_ID is not set.\\n\\nWrangler may deploy to the wrong Cloudflare account on this machine.\\nExport the AYSO Region 13 account ID first, e.g.:\\n\\n  export CLOUDFLARE_ACCOUNT_ID=<ayso13-account-id>\\n\\nFind it in the Cloudflare dashboard sidebar.\\n\");process.exit(1)}else{console.log(\"\\u2713 CLOUDFLARE_ACCOUNT_ID is set\")}'",
    "dev": "npm run preflight && wrangler dev",
    "deploy": "npm run preflight && wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.87.0"
  }
}
